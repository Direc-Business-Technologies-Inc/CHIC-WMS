using System;
using System.Collections.Generic;
using System.Linq;
using System.Runtime.InteropServices;
using System.Text;
using System.Threading.Tasks;
//using com.android.scanner;

namespace Application.Libraries.Utilies
{
    
    /// <summary>
    /// The BarcodeManager.jar was converted to DLL using IKVM.NET instead of importing the JAR file
    /// </summary>
    public class InvengoUtilities: IInvengoUtilities 
    {        
        //private BarcodeManager barcodeManager;        

        //public InvengoUtilities(BarcodeManager.InitResultListerner initRestListener)
        //{
        //    this.barcodeManager = BarcodeManager.getInstance();
        //    this.barcodeManager.init(this, initRestListener);
        //} 
        public void initData()
        {
            
        }
    }
}
